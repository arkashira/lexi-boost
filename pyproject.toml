[project]
name = "lexi-boost"
version = "0.1.0"
description = "Automatic documentation generation for Python repositories."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
