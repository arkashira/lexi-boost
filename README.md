# Lexi Boost

A VS Code extension for detecting bugs in real time.

## Features

* Detects syntax and semantic errors within 1 second of code change
* Highlights detected bugs in the editor with a tooltip
* Bug detection accuracy > 80% on a curated test set

## Installation

1. Clone the repository
2. Run `poetry install` to install dependencies
3. Run `poetry run pytest` to run tests
