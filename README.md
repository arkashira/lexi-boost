# LexiBoost
A domain-specific model training pipeline for generating context-aware code snippets.

## Usage
1. Install the required dependencies using `poetry install`.
2. Run the LexiBoost model using `python -m lexi_boost --repo_path <path_to_repo> --api_specs_path <path_to_api_specs>`.
